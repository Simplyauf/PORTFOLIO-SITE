# PORTFOLIO-SITE
My personal portfolio

---

## Requirements ✅

- **Node.js:** 18.19.0 (pin in `.nvmrc`, `.node-version`, or `NODE_VERSION` in Netlify settings)

> Netlify will honor `.nvmrc`/`.node-version` or a `NODE_VERSION` env var; see project settings if you prefer the UI instead of a file.
